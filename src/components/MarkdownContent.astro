---
/**
 * Prose wrapper for rendered markdown bodies. Pages render the collection entry
 * inside this so all long-form content shares the Kuber prose styles.
 */
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---
<div class:list={['prose', className]}>
  <slot />
</div>
